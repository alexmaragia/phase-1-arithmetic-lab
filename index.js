// Multiplication
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // 31 * 2 = 62

// Generating a Random Integer
const random = Math.floor(Math.random() * 100) + 1; // Generates a random integer greater than 0

// Modulus Operation
const num3 = 28;
const num4 = 6;
const mod = num3 % num4; // 28 % 6 = 4

// Finding the Maximum Number
const max = Math.max(5, 10, 15, 20); // Finds the highest number, which is 20
